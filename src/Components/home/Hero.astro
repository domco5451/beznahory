---
import { getCollection } from "astro:content";
import { Image } from "astro:assets";
import { Icon } from "astro-icon/components";

const allPosts = await getCollection("blog");
---

<div class="swiper-container">
  <div class="nav-arrow nav-prev">
    <Icon class="swiper-button-prev" name="left-arrow"/>
  </div>
  <div class="swiper">
    <div class="swiper-wrapper">
      {
        allPosts.map((post: any, i: number) => (
          <div class="swiper-slide">
            <div class="slide-content-text">
              <h2 class="more-title">Prečítajte si naše blogové príspevky!</h2>
              <h3 class="post-title">{post.data.title}</h3>
              <p class="post-description">{post.data.description}</p>
              <div class="post-links">
                <a class="post-link" href={`/blog/${post.slug}`}>
                  Čítať ďalej &#8594
                </a>
                <a class="post-link-blog" href={`/blog`}>
                  Všetky príspevky
                </a>
              </div>
            </div>
            <Image
              class="hero-image"
              src={post.data.thumbnail}
              alt={post.data.thumbnailAlt}
              loading={i > 0 ? "lazy" : "eager"}
            />
          </div>
        ))
      }
    </div>
    <div class="swiper-pagination"></div>
  </div>
  <div class="nav-arrow nav-next">
    <Icon class="swiper-button-next" name="right-arrow"/>
  </div>
</div>
<style>
  :root {
    --swiper-pagination-color: #172238;
    --swiper-pagination-bullet-size: 8px;
    --swiper-pagination-bullet-width: 15px;
    --swiper-pagination-bullet-height: 15px;
    --swiper-pagination-bullet-inactive-color: #000;
    --swiper-pagination-bullet-inactive-opacity: 0.2;
    --swiper-pagination-bullet-opacity: 1;
    --swiper-pagination-bullet-horizontal-gap: 4px;
    --swiper-pagination-bullet-vertical-gap: 6px;
  }
  .swiper-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    height: 100%;
    background-image: url("../../icons/pozadieAbout.svg");
    background-size: cover;
  }
  .swiper {
    max-width: 1500px;
    flex: 1;
  }
  .nav-arrow {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 60px; /* Fixná šírka pre oblasti šipiek */
    min-width: 40px;
    height: 100%;
    cursor: pointer;
    margin-inline: 20px;
  }
  .swiper-slide {
    display: flex;
    gap: 50px;
    justify-content: space-around;
    padding-inline: 20px;
    padding-block: 100px;
  }
  .slide-content-text {
    display: flex;
    flex-direction: column;
  }
  .hero-image {
    width: 800px;
    height: 350px;
    object-fit: cover;
    border-radius: 20px;
    box-shadow: -30px 30px 10px rgba(0, 0, 0, 0.1);
  }
  .swiper-button-prev,
  .swiper-button-next {
    position: static !important;
    top: 0 !important;
    margin-top: 0 !important;
    width: 60px; /* Kontrola veľkosti ikon */
    height: 60px;
    background: rgba(94, 94, 94, 0.308);
    border-radius: 50%;
    color: #172238;
    transition: color 0.1s ease;
  }
  .swiper-button-prev:hover,
  .swiper-button-next:hover {
    color: #ffffff;
  }
  .swiper-pagination-fraction,
  .swiper-pagination-custom,
  .swiper-horizontal > .swiper-pagination-bullets,
  .swiper-pagination-bullets.swiper-pagination-horizontal {
    bottom: var(--swiper-pagination-bottom, 25px);
    top: var(--swiper-pagination-top, auto);
    width: 100%;
  }
  .post-title {
    font-size: 2.8rem;
    color: #172238;
    margin-block-end: 10px;
    font-weight: bolder;
  }
  .more-title {
    font-size: 1.3rem;
    color: #172238;
    font-weight: 500;
  }
  .post-description {
    line-height: 2rem;
    font-weight: 400;
  }
  .post-link {
    color: white;
    background: #172238;
    padding: 12px 23px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 50px;
    transition: background 0.3s ease, color 0.3s ease;
  }
  .post-link:hover {
    background: var(--hover-bg-secondary-2);
    color: white;
  }
  .post-link-blog {
    position: relative;
    color: #172238;
    font-size: 1.2rem;
    bottom: 0;
    right: left;
  }
  .post-links {
    display: flex;
    gap: 20px;
    margin-top: 20px;
    align-items: center;
    justify-content: space-between;
  }
  .post-link-blog::after {
    content: " ";
    position: absolute;
    width: 100%;
    height: 2px;
    left: 0;
    bottom: -2px;
    background: #172238;
    transition: all 0.3s ease-in-out;
  }
  .post-link-blog:hover::after {
    width: 0;
  }
  .post-link-blog:hover {
    color: var(--text-primary);
  }
</style>
<script src="../../scripts/sliderHero.js"></script>
